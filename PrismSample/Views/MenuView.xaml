<UserControl
    x:Class="PrismSample.Views.MenuView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PrismSample.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="using:Prism.Windows.Mvvm"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <StackPanel Padding="20">
        <Button Content="Navigate - Welcome" Click="{x:Bind ViewModel.NavigateWelcome}" Margin="0,0,0,20"/>
        <Button Content="Navigate - Status Fail" Click="{x:Bind ViewModel.NavigateFail}" Margin="0,0,0,20"/>
        <Button Content="Navigate - Status Success" Click="{x:Bind ViewModel.NavigateSuccess}" Margin="0,0,0,20"/>
        
        <Button Content="Login" Click="{x:Bind ViewModel.Login}" Margin="0,0,0,20"/>
        <Button Content="Logout" Click="{x:Bind ViewModel.Logout}" Margin="0,0,0,20"/>
    </StackPanel>
</UserControl>
